spring:
  application:
    name: "cards"
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
server:
  port: 9000

build:
  version: "3.0"

cards:
  message: "Welcome to cards microservice APIs"
  contactDetails:
    name: "Roshan - Developer"
    email: "roshan@microservice.com"
  onCallSupport:
    - (555) 555-12234
    - (555) 523-12345
